const menuBtn=document.querySelector('.menu');const links=document.querySelector('.links');if(menuBtn){menuBtn.addEventListener('click',()=>links.classList.toggle('open'))}const modal=document.querySelector('.modal');document.querySelectorAll('[data-open-modal]').forEach(b=>b.addEventListener('click',()=>modal.classList.add('open')));document.querySelectorAll('[data-close-modal]').forEach(b=>b.addEventListener('click',()=>modal.classList.remove('open')));if(modal){modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')})}function submitDemo(e){e.preventDefault();const toast=document.querySelector('.toast');toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3600);e.target.reset();if(modal)modal.classList.remove('open')}document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',submitDemo));
